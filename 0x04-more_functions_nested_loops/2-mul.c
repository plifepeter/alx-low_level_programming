#include "main.h"
/**
 * mul - function that multiplies two numbers
 * @a: check the variable a value
 * @b: check the variable b value
 * Return: return a value
 */
int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
