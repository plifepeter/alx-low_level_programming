#include "main.h"
/**
 * add - adding two numbers
 * @na: the first digit to be added
 * @nb: the second digit to be added
 * Return: printing out the value added
 */
int add(int na, int nb)
{
	return (na + nb);
}
