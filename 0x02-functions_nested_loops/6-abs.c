#include "main.h"
/**
 * _abs - Finding the absolute value
 * @e: checking the variable e
 * Return: return the absolute value of integer
 */
int _abs(int e)
{
	if (e > 0)
		return (e);
	else
		return (-e);
}
